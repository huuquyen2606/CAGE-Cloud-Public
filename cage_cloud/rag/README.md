# RAG Support

This folder contains optional retrieval helpers used to inject CVE context into
planning prompts.

The public release treats this as auxiliary context only. Retrieved material
does not become authoritative state and does not replace deterministic evidence
verification.
