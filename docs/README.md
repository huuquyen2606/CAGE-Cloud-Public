# docs

This directory stores repository-authored documentation assets that support the
public release.

- `figures/`: static documentation figures referenced by docs.

Do not treat this folder as a paper replication package or as raw evaluation
evidence unless a file explicitly says so.
